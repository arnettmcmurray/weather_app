// Do NOT put real key here if you plan to commit.
// For local testing, create a new file named `config.local.js` with the same line
// and set your real key. This example only provides a placeholder if local file
// is missing. we arent doing that mistake again
window.OPENWEATHER_API_KEY = window.OPENWEATHER_API_KEY || "YOUR_API_KEY_HERE";
